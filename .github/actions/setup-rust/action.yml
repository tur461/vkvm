name: Setup Rust

description: Prepare Rust environment

runs:
  using: composite

  steps:
    - shell: bash
      run: |
        chmod +x "${{ github.action_path }}/setup.sh"
        "${{ github.action_path }}/setup.sh"
